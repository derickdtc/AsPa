name: aspa
description: A new Flutter project.
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  audioplayers: ^6.5.1
  google_fonts: ^7.0.0
  provider: ^6.1.5
  intl: ^0.20.2
  noise_meter: ^5.1.0
  permission_handler: 12.0.1
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/audios/
